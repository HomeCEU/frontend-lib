version: 2

jobs:
  unit-test:
    docker:
      - image: cypress/base:10
    steps:
      - checkout
      - run: echo "Checkout successful"

workflows:
  version: 2
  build_and_test:
    jobs:
      - unit-test
